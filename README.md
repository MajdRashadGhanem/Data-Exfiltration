# Data-Exfiltration
هذا الكود يمثل سكربت أتمتة لاستخراج البيانات (Data Exfiltration)، حيث يقوم بالانتشار عبر سجل النظام (Registry) لضمان التشغيل الذاتي، ثم مراقبة منافذ الـ USB لنسخ ملفات Excel تلقائياً.
